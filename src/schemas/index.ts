import { 
    movieSchema, 
    movieListSchema, 
    movieCreateSchema, 
    movieUpdateSchema 
} from "./movies.schema";

export default {
    movieSchema, 
    movieListSchema,
    movieCreateSchema, 
    movieUpdateSchema
}